# --- SECRETS & CREDENTIALS (CRITICAL) ---
# Never commit these, even if encrypted. Use a secret manager.
.env
.env.*
*.pem
*.key
*.pub
*.p12
*.pfk
creds/
credentials/
secrets/
# Cloud CLI config captures
.aws/
.azure/
.gcloud/
.kube/config

# --- TERRAFORM / OPENTOFU ---
# Local .terraform directories
**/.terraform/*
# State files (Use remote state for SRE teams!)
*.tfstate
*.tfstate.*
# Crash logs
crash.log
crash.*.log
# Exclude sensitive var files
*.tfvars
*.tfvars.json
# ...but allow example var files
!*.example.tfvars
# Plan files (binary output)
*.tfplan
# CLI config
.terraformrc
terraform.rc
.terraform.lock.hcl

# --- ANSIBLE ---
# Retry files created when a playbook fails
*.retry
# Temporary keys and inventory
*.key
inventory.ini
# Roles installed locally for testing
ansible_collections/
roles/

# --- PYTHON (Scripting & Automation) ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
# Distribution / packaging
dist/
build/
*.egg-info/
# Virtual environments
venv/
.venv/
env/
# Jupyter Notebook checkpoints (common for post-incident analysis)
.ipynb_checkpoints

# --- GOLANG (Tooling) ---
# Binaries
bin/
# Test output
*.out
*.test
# Vendor directory (if you are not committing deps)
vendor/

# --- DOCKER & KUBERNETES ---
# Helm dependency charts
charts/
# Docker local builds
docker-build/
# Kubeconfigs generated by scripts
kubeconfig_*.yaml
config.yaml

# --- EDITOR & OS ---
# macOS
.DS_Store
# VS Code
.vscode/
# IntelliJ / JetBrains
.idea/
*.iml
# Linux temporary files
*~
